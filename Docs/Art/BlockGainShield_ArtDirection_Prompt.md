# Block gain shield — reference-matched revision

Mode: built-in image generation, with the user's existing event-screen and blank UI art as direct visual references.

Output target: `Assets/Art/VFX/BlockGainShield_ArtDirection.png`.

The painted ice/crystal version was rejected and must not be used as the style reference. Keep the established off-white/charcoal palette, fine observation lines, clipped planes, compass motif and subtle paper grain.

## Final prompt

undefined

## Integration

Imported and assigned to BlockGainShield.prefab after approval access recovered. Play verification confirmed the effect is visible at the enemy's screen-space center (0 pixel offset). The previous painted ice version was never assigned. The persistent numerical HUD uses the separate UI_EnemyBlockBadge_WhiteVertex sprite.
